const { productsList,
  productQuery,
  filteredProductList,
  productsIds,
  productById } = require('./products.mocks');

const { salesList } = require('./sales.mocks');

module.exports = { 
  productsList,
  productQuery,
  filteredProductList,
  productsIds,
  productById,
  salesList,
}